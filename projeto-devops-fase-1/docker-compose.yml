version: '1.0'

services:
  web:
    image: projeto-devops-fase-1-web:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./website:/usr/share/nginx/html
    networks:
      - webnet

networks:
  webnet: